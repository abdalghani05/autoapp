<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.grandprojet">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/car"
        android:label="@string/app_name"
        android:noHistory="true"
        android:roundIcon="@drawable/car"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".changer_password"></activity>
        <activity
            android:name=".profil"
            android:label="Profil"
            android:noHistory="true"
            android:parentActivityName=".bottomnavigation" />
        <activity
            android:name=".recherchecat"
            android:parentActivityName=".recherchecat" />
        <activity
            android:name=".voiture"
            android:parentActivityName=".voiture" />
        <activity
            android:name=".MainActivity"
            android:label="Connection"
            android:noHistory="true"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".info"
            android:label="Info"
            android:parentActivityName=".bottomnavigation" />
        <activity
            android:name=".creecompte"
            android:label="Crée compte"
            android:parentActivityName=".creecompte" />
        <activity android:name=".bottomnavigation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>